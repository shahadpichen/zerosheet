node_modules/
.pnpm-store/
dist/
coverage/
.env
.env.*
!.env.example
*.log
.DS_Store
.idea/
.vscode/

# SPIRE bootstrap bundles are generated from the running trust domain. The
# bundle is public, but keeping machine-local control-plane state out of Git
# avoids pretending one developer's root is the production trust anchor.
infra/spire/generated/
