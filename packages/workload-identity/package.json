{
  "name": "@zerosheet/workload-identity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A narrow ZeroSheet adapter around the SPIFFE Workload API and exact peer-identity authorization.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "spiffe": "0.5.1"
  }
}
